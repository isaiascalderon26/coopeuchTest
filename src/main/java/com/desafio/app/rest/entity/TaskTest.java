package com.desafio.app.rest.entity;

/*import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;

import java.util.Date;*/

public class TaskTest {

   /* @Test
    public void testGettersAndSetters() {
        // Crear una instancia de la entidad Task
        Task task = new Task();

        // Establecer los valores de los atributos
        Integer identificador = 1;
        String descripcion = "Tarea de prueba";
        Date fechaCreacion = new Date();
        Boolean vigente = true;

        task.setIdentificador(identificador);
        task.setDescripcion(descripcion);
        task.setFechaCreacion(fechaCreacion);
        task.setVigente(vigente);

        // Verificar los valores de los atributos usando los getters
        Assertions.assertEquals(identificador, task.getIdentificador());
        Assertions.assertEquals(descripcion, task.getDescripcion());
        Assertions.assertEquals(fechaCreacion, task.getFechaCreacion());
        Assertions.assertEquals(vigente, task.getVigente());
    }

    @Test
    public void testToString() {
        // Crear una instancia de la entidad Task
        Task task = new Task();

        // Establecer valores de atributos
        task.setIdentificador(1);
        task.setDescripcion("Tarea de prueba");
        task.setFechaCreacion(new Date());
        task.setVigente(true);

        // Verificar la representación de cadena generada por el método toString()
        String expectedToString = "Task(identificador=1, descripcion=Tarea de prueba, fechaCreacion=..., vigente=true)";
        Assertions.assertEquals(expectedToString, task.toString());
    }

    @Test
    public void testEqualsAndHashCode() {
        // Crear dos instancias de la entidad Task con los mismos valores de atributos
        Task task1 = new Task();
        task1.setIdentificador(1);
        task1.setDescripcion("Tarea de prueba");
        task1.setFechaCreacion(new Date());
        task1.setVigente(true);

        Task task2 = new Task();
        task2.setIdentificador(1);
        task2.setDescripcion("Tarea de prueba");
        task2.setFechaCreacion(new Date());
        task2.setVigente(true);

        // Verificar que las dos instancias sean iguales utilizando el método equals()
        Assertions.assertEquals(task1, task2);

        // Verificar que los hash codes de las dos instancias sean iguales
        Assertions.assertEquals(task1.hashCode(), task2.hashCode());
    }*/
}
